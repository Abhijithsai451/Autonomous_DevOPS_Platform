apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortexops-organization
  namespace: cortexops-system
  labels:
    app: cortexops-organization
    tier: core
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cortexops-organization
  template:
    metadata:
      labels:
        app: cortexops-organization
    spec:
      containers:
        - name: cortexops-organization
          image: cortexops/organization:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: SERVICE_NAME
              value: "cortexops_organization"
          envFrom:
            - configMapRef:
                name: cortexops-common-config
            - secretRef:
                name: cortexops-common-secrets
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            failureThreshold: 10
            periodSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cortexops-organization
  namespace: cortexops-system
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: cortexops-organization