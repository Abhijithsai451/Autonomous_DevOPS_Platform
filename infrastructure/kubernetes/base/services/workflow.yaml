apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortexops-workflow
  namespace: cortexops-system
  labels:
    app: cortexops-workflow
    tier: engine
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cortexops-workflow
  template:
    metadata:
      labels:
        app: cortexops-workflow
    spec:
      containers:
        - name: cortexops-workflow
          image: cortexops/workflow:latest
          command: [ "python", "-m", "uvicorn", "apps.workflow.workflow_main:app", "--host", "0.0.0.0", "--port", "8002" ]
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
              name: http
          env:
            - name: SERVICE_NAME
              value: "cortexops_workflow"
          envFrom:
            - configMapRef:
                name: cortexops-common-config
            - secretRef:
                name: cortexops-common-secrets
          resources:
            requests:
              cpu: "200m"
              memory: "384Mi"
            limits:
              cpu: "750m"
              memory: "768Mi"
          startupProbe:
            httpGet:
              path: /health
              port: 8002
            failureThreshold: 10
            periodSeconds: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cortexops-workflow
  namespace: cortexops-system
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 8002
      name: http
  selector:
    app: cortexops-workflow